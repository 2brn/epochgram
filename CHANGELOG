# Epochgram changelog

## [1.8.21] - 2026-09-07
- Fixed note search highlighting for unquoted multi-word queries.

## [1.8.20] - 2026-09-07
- Fixed linter warning.

## [1.8.19] - 2026-09-07
- Added temporary yellow text highlighting and line focus when opening a matching timeline search suggestion, without modifying Markdown.
- Removed obsolete build-hash packaging and licensing references.
- Resolved lint warnings and updated release/context documentation.

## [1.8.18] - 2026-08-31
- Added a dedicated bell icon on the timeline for ICS calendar sync (synced) events, documented in the README.
- Fixed a caching bug where already-indexed records could lose newly derived flags (e.g. the new sync icon) on reindex/rebuild when file content was unchanged.

## [1.8.17] - 2026-08-20
- Fixed Review all to persist first-run recurring records and report the count of records whose state changed.

## [1.8.16] - 2026-08-09
- Fixed auto-populated required properties for calendar event template.
- Fixed showing only colored circle for Native menus OFF.

## [1.8.15] - 2026-08-06
- Fixed AI summary auto-enqueue gating so background create/modify/rename activity respects Auto summarize off and no longer starts the AI bridge unintentionally.

## [1.8.14] - 2026-08-05
- Fixed duplicated records after Calendar sync.
- Unified Calendar sync YAML keys to match template placeholders.
- Added source URL sanitization to prevent storing secrets while preserving a safe, rewritten format.

## [1.8.13] - 2026-08-04
- Fixed Mark context-menu labels so Native menus consistently show Mark items and color subitems.
- Removed one extra line as a minor cleanup.

## [1.8.12] - 2026-08-02
- Added **Epochgram: Toggle review for current file** to switch all file records between Draft and Reviewed.
- Refined edit handling so only anchor records reset from Reviewed to Draft; content, tracked-change, and recurring records preserve their review state.
- Added `nodates:` as an alias for `noparsed:`, plus `$same` and `!same` aliases for current-file search.
- Validated AI Bridge YAML when the page opens and reject native backends in Obsidian Web Viewer mode.

## [1.8.11] - 2026-08-01
- Fixed styles for What's new page for mobile view.

## [1.8.10] - 2026-08-01
- Normalized bundled text assets to read from git blobs so repeated builds keep `main.js` byte-identical across WSL/Linux and GitHub.
- Kept the timestamp-free settings footer and README/context text aligned with the bundled output.

## [1.8.9] - 2026-08-01
- Removed the build timestamp from the bundled output and settings footer so repeated production builds produce the same `main.js`.
- Updated the README and repo context to match the timestamp-free settings footer.

## [1.8.8] - 2026-07-31
- Updated the release and production-build workflows to current GitHub Actions versions and Node 24, removing the Node 20 deprecation warning.
- Kept release packaging behavior aligned with the production build so published bundles stay consistent with local builds.

## [1.8.7] - 2026-07-30
- Refined Pro settings defaults and reset behavior so activation state stays predictable.
- Fixed calendar-sync UI so Pro-off values stay blank until Pro is active again, while preserving restored values.
- Added a regression test covering the Pro-off calendar-sync UI behavior.

## [1.8.6] - 2026-07-30
- Added quick command-palette toggles for timeline filters (reviewed, tracked changes, content dates, attachments, Epochs).
- Updated UI/docs terminology from parsed content to content dates and aligned command wording.
- Limited Epoch search suggestions to appear only when Epochs view is active.
- Improved production packaging/hot-reload behavior with in-place file updates and `.hotreload` handling.
- Added What's New page for 1.8.6.

## [1.8.5] - 2026-07-29
- Hardened npm dependency security with overrides and lockfile updates to clear audit vulnerabilities.
- Added macOS-specific pin badge label alignment.

## [1.8.4] - 2026-07-28
- Fixed AI bridge model download progress so native Chrome download state now attaches a monitor and updates progress correctly.
- Normalized native Summarizer fallback language order to prefer `en` before other supported languages.

## [1.8.3] - 2026-07-28
- Refined AI bridge language support to cover `de`, `en`, `es`, `fr`, and `ja` across settings validation, runtime defaults, and docs.
- Updated the AI bridge to pause jobs until the native model is ready and clear download-related errors when the model state changes.
- Stopped enabling track changes automatically on first Pro activation.
- Refined What's New view spacing and synchronized repo context/docs updates.

## [1.8.2] - 2026-07-27
- Fixed What's New startup selection: once a newer version page has already been shown, older pages are no longer auto-shown.

## [1.8.1] - 2026-07-24
- Removed `!important` from invalid URL input style by increasing selector specificity.

## [1.8.0] - 2026-07-24
- Added Calendar sync support for ICS feeds.
- Fixed: pin fly animation, **⌘ Epochgram: Review all**.
- Improved record ordering for the same day.
- Updated README, what's new.

## [1.7.2] - 2026-07-23
- Fixed What's New startup fallback so existing users still see the latest available unseen page when current release notes page is absent.
- Updated What's New decision tests to cover fallback and future-version guard behavior.
- Refined 1.7.0 What's New content wording.

## [1.7.1] - 2026-07-23
- Fixed pin badge mobile shape regression and restored consistent badge silhouette across devices.
- Refined badge hover rendering to reduce seam/text-shift artifacts.
- Hardened transitive dependency security with targeted overrides and lockfile updates.
- Fixed an empty block statement in plugin lifecycle startup handling.

## [1.7.0] - 2026-07-23
- Added pin modes (`today`, `date`, `dock`) with improved pin badge interactions, including touch and drag behavior updates.
- Moved pin and mark state into note YAML frontmatter, including explicit mark hex colors for consistent visible state.
- Added AI bridge opening in the right side-bar webviewer and improved AI bridge stability.
- Improved search and UI consistency with better recent-note suggestion priority and settings wording refinements.
- Updated badge visuals and refreshed the What's New page for 1.7.0 with release illustrations.

## [1.6.1] - 2026-07-12
- Added fallback handling for AI bridge disconnect when closing the Obsidian Web viewer tab.
- Fixed AI bridge server HTTP module loading path via `require("http")` for desktop runtime.

## [1.6.0] - 2026-07-12
- Added cloud backend support for AI bridge summarization and updated related runtime/polyfill flow.
- Added and refined opening AI bridge in Obsidian Web viewer, including startup/open behavior updates.
- Refactored AI bridge settings/validation paths and updated README wording/documentation.

## [1.5.9] - 2026-07-09
- Stabilized startup timeline snap positioning to eliminate micro-jitter on Obsidian startup.
- Preserved world-anchor alignment during canvas resize/reposition.
- Unified date targeting to choose records nearest to Today for recurring/non-recurring entries.

## [1.5.7] - 2026-07-08
- Fixed review-state synchronization for recurring records/context menus, reset reviewed -> draft on real edits, and updated defaults (anchor mdate ON, first Pro title threshold = 1.0).

## [1.5.6] - 2026-07-07
- Fixed review-state persistence so Review all remains stable after reload.
- Extended Review/Draft/Hide propagation to recurring synthetic records.
- Updated file and folder context-menu Review/Draft/Hide actions to apply recurring state updates consistently.

## [1.5.5] - 2026-07-04
- Fixed icon sizing.

## [1.5.4] - 2026-07-04
- Fixed AI Bridge startup fallback and improved settings DOM element creation stability.
- Cleaned up AI Bridge module loading and removed obsolete module-name normalization.
- Improved AI bridge startup state handling to avoid stale bridge references on startup failures.
- Fixed multiple lint warning issues across the codebase.
- Improved N+ badge rendering and platform detection behavior.

## [1.5.3] - 2026-07-03
- Added compact (+n) record click/tap cycling behavior and reset logic when active file changes.

## [1.5.2] - 2026-07-02
- Added `$current` search filtering for the active file and updated search suggestion actions to lowercase.
- Added folder explorer actions for Review, Draft, and Hide; removed the obsolete file-level Show action.

## [1.5.1] - 2026-07-02
- Renamed the **Anchor by mdate** setting label to **Anchor mdate** across UI and docs.
- Renamed **Search results** setting label to **Search results count** for clarity.
- Added a total downloads badge to the README release header.

## [1.5.0] - 2026-07-01
- Refactored a large number of lint warnings and errors, including UI label casing updates.
- Fixed two-digit year parsing and date format positioning issues.
- Added a **Search results** slider to control suggestion count.
- Expanded search behavior to work across all files with query-aware filtering.
- Added same-folder similarity via **Title threshold = 1.0** and updated record/search ordering by modified date.

## [1.4.20] - 2026-06-17
- Fixed lint warning issues across plugin.
- Improved popout window compatibility by using window/activeDocument-safe APIs.
- Updated production build/deploy script behavior and release metadata synchronization.

## [1.4.19] - 2026-06-05
- Metadata-only patch release (version/changelog sync).

## [1.4.18] - 2026-06-05
- Increased today distance indicator opacity for improved timeline visibility.

## [1.4.17] - 2026-06-02
- Fix: AI bridge auto-start after Pro activation/validation when AI features are enabled.

## [1.4.16] - 2026-05-31
- Drafts filter state: session-only (no persistence in data.json).

## [1.4.15] - 2026-05-24
- Remove legacy compatibility shims; enforce canonical alias handling in search/bridge flows.
- Stop mirroring explicit topics to `epochgram-topics.json`.

## [1.4.14] - 2026-05-21
- Timeline viewport: updated bounds to 1000...2100 years.
- Progress bar: updated wording.

## [1.4.13] - 2026-05-20
- Updated README hero/content and examples.
- Stopped timeline inertia when collapsing sidebar on mobile swipe.
- Prevented timeline pan/scroll during dominant right-swipe collapse gestures on mobile.

## [1.4.12] - 2026-05-20
- Fixed blinking state for AI summaries.
- Added more README examples.
- Added/updated tests.

## [1.4.11] - 2026-05-19
- Fixed indexing for cached YAML dates and filename dates.
- Added support for full weekday names.
- Renamed Settings labels.
- Updated README.

## [1.4.10] - 2026-05-18
- Fixed disappeared AI summaries after index refresh.
- Fixed Shift/Alt zoom anchoring and fallback behavior, including scroll-nav vs opened-record date handling.

## [1.4.9] - 2026-05-18
- Added **Anchor by mdate** setting: use mtime instead of ctime as the anchor fallback for files with no filename date or YAML date property.
- Fixed hover right-column shift incorrectly triggering when hovering over a single-row record (no neighboring rows at the same Y position).
- Fixed `yamlDateProperty`, `yamlDescriptionProperty`, and `anchorMdate` settings not persisting across reloads.
- Updated AI feature descriptions ("using local Chrome AI" wording).

## [1.4.8] - 2026-05-18
- Updated manifest and README.

## [1.4.7] - 2026-05-18
- Settings UI: renamed frontmatter-related labels for clarity (Anchor property, Summary property, Parse all properties).

## [1.4.6] - 2026-05-17
- Fix settings autofocus behaviour.

## [1.4.5] - 2026-05-17
- Added **Record width limit (%)** setting to control when timeline rows collapse overflow into `(+n)`.
- Reworked settings layout into clearer sections.
- Updated summary rendering/measurement to respect record-width limits at runtime and tuned summary font zoom scaling.
- Improved mobile touch behavior: smoother inertia carry between swipes and right-swipe sidebar collapse handling.
- Improved interactions and README/docs wording.

## [1.4.4] - 2026-05-17
- Fix mobile Epochgram opening in side panel leaf.

## [1.4.3] - 2026-05-16
- Fixed auto-activating view if `Open timeline on startup` is on.

## [1.4.2] - 2026-05-16
- Added handling for Ctrl/Cmd+Enter and Ctrl/Cmd+Click to open matched records in a new tab.

## [1.4.1] - 2026-05-16
- Fixed timeline startup snap alignment so auto-open startup behavior matches manual open behavior.
- Updated plugin/runtime support paths related to trust/device and base64 handling.
- Refined README docs: updated download links and removed leftover comments.

## [1.4.0] - 2026-05-16
- Reworked AI Bridge settings into YAML and improved summarization flow; added `Summarize current file` command.
- Added bridge and epochs fixes.
- Refactored review-related code paths.
- Fixed and cleaned up styles.
- Updated README.

## [1.3.17] - 2026-05-14
- Add support for parsing ISO-like timestamps.

## [1.3.16] - 2026-05-12
- Fixed `npm ci` dependency resolution conflict between Vite and esbuild.
- Synced manifest after local build updates.

## [1.3.15] - 2026-05-12
- Fixed failed tests and build.
- Updated AI summary/epoch tests for constructor-safe `Notice` mocking and missing bridge stubs.
- Fixed modal lifecycle floating-promise lint issues in suggest modals.
- Added `package-lock.json` and refreshed dependency metadata.

## [1.3.14] - 2026-05-12
- Fixed release workflow YAML syntax: corrected env indentation and removed release-only repo migration.

## [1.3.13] - 2026-05-12
- Converted to public repository: removed closed source references and simplified release workflow to publish directly to `2brn/epochgram`.

## [1.3.12] - 2026-05-12
- Added cryptographic artifact attestations to GitHub Release workflow for build provenance verification.
- Reduced GitHub Release assets to only Obsidian-supported files (manifest.json, main.js, styles.css).

## [1.3.11] - 2026-05-12
- Replaced note-name toggle with `Filename length` word-based truncation (`0-12`) and updated rendering flow.
- Fixed filename truncation across languages/scripts (including Cyrillic, Greek, Arabic, Chinese, Japanese) and punctuation handling.
- Fixed cases where summaries equal/prepend filenames (including empty-content and tracked-change fallback paths) so truncation is applied consistently.
- Enforced display-time `Summary length` truncation for tracked-change summaries, including prefixed-summary paths.
- Summary word counting now ignores `↗︎`, `[x]`, and `[ ]` markers.

## [1.3.10] - 2026-05-12
- Fixed double-tap handler to use first tap coordinates instead of second tap coordinates for consistent gesture recognition.

## [1.3.9] - 2026-05-11
- Added handling Daily Notes setting Template file location for creating daily notes.
- Fixed refreshing index on changed YAML.
- Updated readme.

## [1.3.8] - 2026-05-08
- Manual YAML `description` field is now used as-is in summaries — punctuation such as `!`, `?`, `;` is preserved and no markdown flattening is applied.

## [1.3.7] - 2026-05-08
- Renamed `CHANGELOG.md` to `CHANGELOG` (no extension); updated settings link, release workflow, and production build to match.
- Fixed docs site to rewrite `[CHANGELOG](CHANGELOG)` links to the absolute GitHub URL.

## [1.3.6] - 2026-05-07
- Fixed cross-device index ping-pong between desktop and mobile: preserve `cdate` for binary files, strip runtime-only fields (`indexedMtimeMs`, `indexedSize`, `__pendingTrackedReviewStateClear`) from saved index.
- Added version display to settings.
- Immediate timeline navigation on mobile without scroll animation.
- Updated disable reasons.

## [1.3.5] - 2026-05-05
- Summaries now flatten HTML content (strips tags, decodes entities, removes `<style>`/`<script>` blocks).
- Links, wiki-links, embeds, and images in summaries now render with a `↗︎` link arrow prefix; `<img alt>` shown as alt text.
- Preserved checkboxes (`- [ ]` / `- [x]`) in summary output.
- Fixed trailing link arrow appearing at truncation boundary; prevented duplicate `...` ellipsis.
- Updated search tips and pro benefits copy.
- Desktop-only fixed width for Date/Description property name inputs in settings.

## [1.3.4] - 2026-05-04
- Search: added `$marked`, `$hidden`, `$similar` as aliases for the `!marked`, `!hidden`, `!similar` filter tokens.
- Fixed title similarity threshold to respect a configured value of `0` (disabled) without falling back to the built-in default.
- UI: lowercase "disabled" label for title threshold slider in Pro settings.

## [1.3.3] - 2026-05-04
- Added configurable YAML date property name setting (replaces hardcoded `date`).
- Added configurable YAML description property name setting (replaces hardcoded `description`).
- Respects Daily Notes > Custom format hours/minutes/seconds in daily note file names.
- Date overlay on timeline always displays in `YYYY-MM-DD` format.

## [1.3.2] - 2026-05-03
- Prioritized YAML description for epoch generation input over AI/content summaries.

## [1.3.1] - 2026-05-03
- Fixed stale Pro settings status so license verification updates in-place without reopening Settings.
- Added regression test coverage for startup license validation UI refresh behavior.
- Naming cleanup and consistency updates.

## [1.3.0] - 2026-05-03
- Search: added !hidden directive — shows only hidden records; in Epochs view
  shows only epochs with hidden children (rows rendered muted).
- Search: added !similar directive — shows records from the active file and
  semantically similar files; works correctly in Epochs view.
- Filters: review and parsed filters are now 2-state toggles (Drafts on/off,
  Parsed on/off); state persisted as showDraftsOnly/showParsed.
- Settings: added "Parse dates in properties" toggle (parseDatesInFrontmatter).
- Settings: removed "Simple mode".
- Export Epochs: removed standalone HTML export feature.
- Timeline: cursor switches to hand pointer only after the pointer moves.
- Updated Readme.

## [1.2.2] - 2026-05-01
- Timeline: middle-mouse drag panning, updated cursor.

## [1.2.1] - 2026-05-01
- Timeline: avoid duplicate note-name rendering when summary length is disabled and Show note name is enabled.
- Tooling: migrate Vitest config to ESM and update Vitest/Vite test dependencies.
- Docs: README update.

## [1.2.0] - 2026-05-01
- Filters: Edits and Epochs filter buttons are now hidden when unavailable (Pro inactive, track changes off, or no epoch entries), instead of shown as disabled.
- Menus: Topic context menu item is now hidden entirely when Pro is inactive or topic threshold is disabled, instead of shown as disabled.
- Menus: Summary context menu item is now hidden when summary length is set to 0.
- Timeline: Records with summary length 0 now show filename-only for all record types (including tracked entries), not just anchors.
- Menus: Renamed "Edit summary…" to "Summary…"; removed it from the file explorer context menu.

## [1.1.14] - 2026-04-30
- Mobile: timeline scroll-to-opened now updates the position immediately when a file is opened, even while the timeline panel is collapsed.
- Readme: minor documentation update.

## [1.1.13] - 2026-04-30
- Indexer: date-only lines (e.g. `April 04 2026`) now create timeline records instead of being silently dropped.
- Indexer: content-date entries are no longer filtered out when summary length is set to 0; show note name as fallback summary instead.
- Settings: summary length slider double-click reset no longer fires two index-refresh notices.

## [1.1.12] - 2026-04-30
- Indexer: fix date parsing for compact `YYYYMMDD`, US `Month DD, YYYY`, and `DD-Mon-YY[YY]` formats.

## [1.1.11] - 2026-04-29
- Timeline/EPOCHS: fix ALT-release restoring hover after Alt+Up/Down and Alt+Wheel.

## [1.1.10] - 2026-04-28
- Recurring: if no start date use note anchor date.
- File context menu: updated review items to match record context menu.
- Scroll: suppress actions during scroll (desktop and mobile).
- UI: fixed strings to match one format `Epochgram: Details...`.

## [1.1.9] - 2026-04-25
- Protocol/docs: update obsidian:// handler and BRAT link.

## [1.1.8] - 2026-04-25
- Fix Drafts filter results count.

## [1.1.7] - 2026-04-24
- Reviews: automatically move only tracked changes to Draft.
- Reviews: fix preserving Hidden state.
- AI Bridge: hide the AI Bridge button when the server is not started.

## [1.1.6] - 2026-04-21
- Docs: add GIF previews + README updates.
- Timeline: ctrl-wheel zoom animation; interrupt scroll animation on direction change.
- Pro AI Bridge: tighten security.
- Similarity/Semantics: fix gating + refresh highlights; force semantic refresh on metadata change; update default topics model/list.
- Tests: fix epoch-view test.

## [1.1.5] - 2026-04-18
- Model-download UX: fewer notices, remove error stack, open Hugging Face externally.

## [1.1.4] - 2026-04-18
- Timeline: fix anchor drag-and-drop so filename-date notes get renamed to the dropped date (keeps title/filename in sync).
- Epochs/AI summaries: exclude empty daily notes from generation.

## [1.1.3] - 2026-04-18
- Mobile: fix long-tap switching to epochs.

## [1.1.2] - 2026-04-17
- Pro defaults: use shared reset constants for first-time similarity threshold and zero-shot minimum score values.

## [1.1.1] - 2026-04-17
- Timeline: wheel scroll now animates at constant speed instead of easing/exponential falloff.

## [1.1.0] - 2026-04-17
- Timeline: wheel scroll now animates at constant speed (no easing/exponential falloff).
- Timeline: hover is suppressed during wheel pan (matching zoom behavior) and re-engages on pointer move.
- Timeline: increase base row spacing from 90 to 100 for better readability.

## [1.0.9] - 2026-04-17
- UI/Mobile: throttle model downloading progress notices to 10s interval (matching semantics/topics progress notice frequency).

## [1.0.8] - 2026-04-17
- UI/Mobile: add model downloading progress notices for similarity models on mobile devices (same timeout behavior as semantics/topics processing).
- Search/UX: refine HuggingFace topics model search filter to show more relevant Xenova zero-shot models.

## [1.0.7] - 2026-04-17
- AI queue/epochs: fix cancel race paths that could keep enqueueing work after clearing progress.
- CI/release: set `ONNXRUNTIME_NODE_INSTALL=skip` for workflow installs to avoid optional `onnxruntime-node` NuGet download failures.

## [1.0.6] - 2026-04-17
- Similarity: update default semantics/topics models, improve model picker suggestions, and add model-downloading notices when changing models.
- Runtime/UI: improve worker/runtime handling for semantics/topics models and refine status bar progress/tooltip behavior.
- Pro/UI: update Pro activation loading animation and preserve scroll position.
- Docs/context: refresh README and Context Pack entries for model defaults and thresholds.

## [1.0.4] - 2026-04-16
- Timeline search: add `!marked` to restrict search/filter to marked records (including inherited marks).
- UI: keep the date overlay label updated during mouse-wheel scrolling.
- Docs: update README to reflect `!marked`.

## [1.0.3] - 2026-04-16
- Recurrence: support “every month on -D” month-day rules (end-of-month recurrence).
- Timeline: refresh virtual/pinned items across midnight to prevent day-shifted entries.
- UI: capitalize “Epoch”/“Epochs” across menus and commands.
- AI/Pro: simplify “Generate/Export Epochs” labels and notices.

## [1.0.2] - 2026-04-15
- Similarity/topics: fix processing and update semantics/topics input + defaults.
- AI bridge: remove Node built-ins (`crypto`, `fs`, `path`, `child_process`) and satisfy lint/build constraints.
- UI: limit max width of the record context menu.
- Dev/build: lint rule updates and cleanup.
- Docs: README updates.

## [1.0.1] - 2026-04-12
- Dev/build: clear unused artifacts and add eslint-plugin-obsidianmd.

## [1.0.0] - 2026-04-11
- First public release.
- Commands: remove plugin ID prefix from command IDs.

## [0.7.0] - 2026-04-11
- Pro licensing: sync Pro certificate verify key/id (epochgram-20260411).
- Export: update standalone export bundle.
- Docs: update README.

## [0.6.5] - 2026-04-09
- Mobile: double-tap date reliably creates the next daily note variant (matches desktop double-click behavior).
- Menus: record/epoch/date context menus now use a header title row instead of duplicating the name as the first item.
- UI: menu header shows vault-relative path and uses consistent styling.
- Docs: refresh README interactions and menu descriptions.

## [0.6.4] - 2026-04-08
- Summaries: editing a recurring record summary now updates the file’s anchor record (and recurring occurrences reflect the override).

## [0.6.3] - 2026-04-08
- Reviews: remove legacy per-file-per-day review overrides; persist Reviewed state per record (including virtual recurring occurrences).
- Metadata: update package description string.

## [0.6.2] - 2026-04-08
- File tree: simplify context menu to a single “Show/Hide” toggle (preserves review state) and remove the topic editor entry.
- Timeline: refine anchor drag-and-drop rules (pinned-today clone constraints; filename date-range drops update the first date).
- Indexing: tighten tracked-date behavior and tracked/frontmatter interactions.
- Menus/UI: normalize review actions and refresh summary icon behavior.
- Docs/assets: refresh README + Context Pack notes and update bundled icons/export artifacts.

## [0.6.1] - 2026-04-07
- Timeline: drag-n-drop anchor records to change date (mouse + touch, auto-scroll), updating YAML `date` and renaming daily notes.
- Docs/context: refresh README and Context Pack.

## [0.6.0] - 2026-04-04
- Scroll navigation: when navigating a marked file, constrain semantic-related “similar” expansion to the same mark color group.

## [0.5.10] - 2026-04-04
- Pro activation: improve `obsidian://epochgram-pro` link handling (prefill key, disable button with “Activating…”, and show clearer failure messages).
- Docs: update README.

## [0.5.9] - 2026-04-04
- Timeline: scale the “how far from Today” indicator by months (BASE_SPACING/month) for both past and future.

## [0.5.8] - 2026-04-03
- Reviews: move Draft/Reviewed state to per-record and update menus/commands accordingly.
- Recurrence: accept frontmatter `recur:` as an alias for `repeat:`.
- Assets/docs: update README, add circle-today icon, and refresh bridge image.

## [0.5.7] - 2026-04-02
- Timeline: use the date label color for the how-far indicator.
- Dev/build: ignore the generated `export-epochs-standalone.js` bundle.

## [0.5.6] - 2026-04-02
- UI/docs: update how-far indicator styling and refresh README.

## [0.5.5] - 2026-04-02
- Timeline: add a small red horizontal marker at the focus anchor to show where “how far from Today” begins.

## [0.5.4] - 2026-04-02
- Pro licensing: suppress the startup validation failure notice when the cached Pro certificate still keeps Pro active (offline/transient errors).

## [0.5.3] - 2026-04-01
- Settings/UI: add **Simple mode** to reduce menus and visual noise.
- Timeline: add a thin red indicator showing how far the view is from Today (scaled so full height is one year).
- Navigation: refine active-file snap selection when recurring records are present.
- Menus/filters: simplify context menus in Simple mode and fix parsed-content filter cycling when toggling Simple mode.
- Docs/context: update README and Context Pack notes.

## [0.5.2] - 2026-04-01
- AI Bridge: refresh bridge page title and chart styling.

## [0.5.1] - 2026-03-31
- Date overlay: show the hovered record/date’s day (not the scroll anchor day).
- Date math: make dayIndex/date conversions DST-safe to avoid off-by-one day behavior.
- Notices: auto-dismiss informational notices (keep error notices persistent).

## [0.5.0] - 2026-03-31
- Pro licensing: minor tweaks to license handling and reactivation restore coverage.
- Docs/context: refresh README and Context Pack notes.

## [0.4.11] - 2026-03-29
- Timeline search: for empty input, show only recent items that resolve to real searchable entries (exclude folders).

## [0.4.10] - 2026-03-28
- Timeline search: show recently opened files for empty input and de-dupe suggestions.
- Release tooling: add scripts to sync Pro keys from the web repo and sync the release README.
- Export: rebuild standalone export bundle.
- Docs/context: update README and Context Pack notes.

## [0.4.9] - 2026-03-27
- Pro: validate license at most once per day on startup, with local checks when offline.
- Pro: prevent time rollback from bypassing startup license validation.
- Notices: handle uncaught errors with persistent notices.
- Docs: refresh Timemap naming/wording.

## [0.4.8] - 2026-03-26
- Indexing: support YAML frontmatter `noindex` to exclude a file from timeline/search indexing.

## [0.4.7] - 2026-03-25
- Pro: always show a one-time-per-session notice when startup license validation fails.

## [0.4.6] - 2026-03-25
- Pro: add an opt-in startup notice when Pro validation fails (shown at most once per app session).
- Docs: update the Context Pack for the Pro/license settings.

## [0.4.5] - 2026-03-25
- Pro/license UX: blur the stored license key preview and show “Reactivate” whenever a prior key exists (including after startup validation failures).

## [0.4.4] - 2026-03-25
- Pro: switch to certificate-based entitlements.
- Pro: drop buildHash trust binding.
- Packaging: trim production artifacts (copy only README-referenced images; ignore stray zips).
- Docs: update README.

## [0.4.3] - 2026-03-23
- Pro activation: keep device-bound activation local per device, follow backend redirects during activation, and preserve the startup-only monthly validation flow with daily retry fallback.
- Pro runtime gating: stop mutating synced settings for Pro/free transitions by moving feature gating to runtime-only effective state.
- Settings and maintenance: allow manual AI summaries rebuild even when Auto summarize is off, add reactivation confirmation, and remove the persisted backend base URL from normal settings.
- Mobile/license UX: restore the generic activation failure notice, keep the License row to a single Pro status line, and avoid persisting transient activation errors.
- Docs/tests/context: update the Context Pack and add regression coverage for activation persistence, validation cadence, redirect handling, and maintenance rebuild behavior.

## [0.4.2] - 2026-03-23
- Pro activation: keep device-bound activation state local per device, add offline grace handling, and switch validation to startup-only monthly checks.
- Pro settings UI: restore the masked key preview locally, refine focus/blur behavior, tighten status text, and make the license input/button stack when space is tight.
- License messaging: remove device counts from notices and simplify the device-limit message to contact support.
- Release tooling: allow manual release publishing again.
- Docs/tests/context: update README/context-pack wording and add regression coverage for the new activation persistence and validation flow.

## [0.4.1] - 2026-03-22
- Pro licensing: switch activation to backend claim-key/token verification, remove the legacy local license generator, and preserve only a masked key preview in settings.
- Pro settings UI: refine the license row with clearer status text, inline last-check display, compact desktop layout, and full-width mobile input behavior.
- AI bridge and branding: refresh bridge assets/logo usage and update packaged imagery.
- Release tooling: harden release-sync/push-both handling and keep production packaging aligned with the release repo flow.
- Docs/tests: update README/context wording and add regression coverage for the new Pro activation flow.

## [0.4.0] - 2026-03-19
- Versioning: reset plugin major versioning to 0.x.y and sync tags/docs.
- Metadata: bump minimum Obsidian version to 1.12.4.

## [0.3.53] - 2026-03-17
- Summaries: prefer YAML frontmatter `description` for tracked-change entries.
- Timeline navigation: ignore recurring records when normal matches exist; when only recurring is visible, anchor/snap to the oldest recurring.

## [0.3.52] - 2026-03-17
- Epochs UI: normalize "Epoch(s)" casing across menus, notices, and settings.
- Commands/docs: rename the AI bridge command to “Open AI bridge”.

## [0.3.51] - 2026-03-17
- Epochs view: fix an edge case where a pinned-today synthetic clone could make an epoch visible even when Attachments are hidden.
- Page preview: cancel the delayed hover preview if the cursor/hover target changes before the delay elapses.

## [0.3.50] - 2026-03-16
- Timeline navigation: reset scroll-nav target/anchor on index refreshes and timeline view changes (filters/settings/view toggles).
- Summaries: prefer YAML frontmatter `description` as the base text for normal (non-AI) summaries, including dateprop anchors.
- Review state: preserve hidden entries across index refresh/reprocess flows.
- Export: rebuild standalone export bundle.

## [0.3.49] - 2026-03-16
- Pro/recurrence: gate `repeat:` expansion to Pro and refresh synthetic recurring entries immediately on Pro activation/deactivation.
- Tests/docs: add regression coverage for Pro deactivation and update the context pack.

## [0.3.48] - 2026-03-16
- Timeline navigation: improve scroll-nav focus/reset behavior across mouse/touch/wheel interactions.
- Indexing/summaries: refine entry aggregation and recurring/frontmatter handling; add regression coverage.

## [0.3.47] - 2026-03-16
- Updated logo and text.

## [0.3.46] - 2026-03-15
- Epochs view: open a related record by preferring the newest eligible day in the epoch range (and keep within-day source preferences).
- Timeline search (Epochs): show matching normal (non-epoch) records in suggestions alongside current-bucket epoch matches.

## [0.3.45] - 2026-03-15
- Epochs view: re-anchor packed epochs around the 33% focus baseline when the anchor’s date-aligned target is offscreen (animated shift + anchor lock to avoid jitter); rebuild standalone export bundle.

## [0.3.44] - 2026-03-14
- Summaries: harden the normal-summary “formatted start” heuristic to reduce false positives from URLs, inline-code/identifier-like tokens, and mid-word `*` patterns; adds regression coverage.

## [0.3.43] - 2026-03-14
- AI summaries: tighten grouping/deduping so re-enqueued jobs replace older queued/in-flight work for the same logical group.
- Hashing: keep anchor AI-summary hashes stable while accepting legacy stored hashes for backward compatibility.
- Summaries: always prefer YAML frontmatter `description` for tracked-change entries (including during later re-aggregation when file text isn’t cached).
- Epoch reduce: lower `AI_REDUCE_MAX_DEPTH` to 4 to bound reduce follow-ups.

## [0.3.42] - 2026-03-14
- Pinning: keep YAML frontmatter `description`-based anchor summaries stable after re-aggregation (adds regression coverage).
- Pin animation: use accelerate easing for the fly-to-Today effect.

## [0.3.41] - 2026-03-14
- Daily notes: allow multiple notes per day using ` (n)` variants; timeline click opens the first available note and double-click always creates the next one (with YAML `date`).
- Date menu: always shows the next daily note filename and always offers “Create daily note”.
- Summaries: prefer YAML frontmatter `description` for anchor entries (e.g. `dateProp`) and treat YAML-only notes as empty (fallback to filename).

## [0.3.40] - 2026-03-13
- Timeline search (Epochs): keep the search results label in sync when zoom changes the active epoch bucket.

## [0.3.39] - 2026-03-13
- Persistence (Android): make `epochgram-index.json` serialization deterministic and skip no-op rewrites so it doesn't change on every app start.

## [0.3.38] - 2026-03-13
- Indexing: add YAML frontmatter `repeat:` schedules (friendly formats + RRULE) as virtual timeline entries; improve summaries by preferring frontmatter `description` for content, ranges, and recurring entries.

## [0.3.37] - 2026-03-13
- AI summaries: fix filename date-range range-day records so context-menu summarization both enqueues and applies results correctly.
- AI bridge: update the built-in default Summary/Epoch context templates and set epoch defaults to `headline` + `long`.
- AI bridge UI: Reset restores built-in templates; status bar shows a red disconnected indicator when the bridge page isn’t connected.
- Bridge page: remove unused placeholders and add a parse-only script syntax regression test.

## [0.3.36] - 2026-03-12
- Settings: refactor Generate Epochs to a boolean (`true` enabled, `false` disabled) while still accepting legacy saved string values at load time.
- AI summaries: standardize the queued notice wording to match the existing “AI summaries: queued …” format.
- Tests/docs: update fixtures and the context pack to match the new boolean semantics.

## [0.3.35] - 2026-03-12
- Epoch inputs: when AI summary generation errors for a record, fall back to the normal (non-AI) summary for that record.
- Rebuild → Epochs: show queued notices reliably in Fast mode (including when internal per-note summaries are queued first).
- AI bridge (Epochs): update defaults (type `headline`, length `long`) and refine the default epoch context template.

## [0.3.34] - 2026-03-11
- AI bridge: separate Summary vs Epoch controls (type/length/languages/context) and tighten placeholder handling/layout; defaults are `headline` + `long`.
- Epochs (Fast): persist/reuse per-note AI summaries for epoch inputs; forcing epoch regeneration no longer forces re-summarizing notes.
- Epoch navigation: skip synthetic pinned-today clones and prefer anchor dates when opening notes from epochs.

## [0.3.33] - 2026-03-09
- Settings/Pro: removed the legacy “Show AI summaries” toggle and replaced Generate Epochs with `No` / `Fast (summaries)` / `Detailed (full content)` modes.
- Epochs (Detailed): fixed missing-epochs prompting/scheduling so it stays responsive and uses the same cascade strategy as Rebuild → Epochs (avoids runaway token planning).
- Indexing: fixed filename date-range anchoring so a range file doesn’t also appear on the file’s `cdate` day.
- Mobile: progress notices for Semantics/Topics now respect the 10s grace period starting from actual processing start (not queue scheduling time).
- Tests/docs: added regression coverage and updated the context pack for the above changes.

## [0.3.32] - 2026-03-03
- Timeline search: empty input suggests last query results per session, clearing the input resets cached suggestions, and progress notices use a longer grace period.
- Rendering: switch summary separator from `•` to `⸱` and keep trimming tolerant of both symbols.

## [0.3.31] - 2026-02-28
- AI bridge: add persisted Summarizer controls (type/length + language lists) and update bridge runtime options accordingly.

## [0.3.30] - 2026-02-26
- AI contexts: refine output instruction wording for summaries and epochs.

## [0.3.29] - 2026-02-26
- AI contexts/docs: remove unused reduce context template and update the context pack.

## [0.3.28] - 2026-02-26
- Epoch AI prompt: tighten the Rules to discourage vague meta phrasing.

## [0.3.27] - 2026-02-26
- AI bridge: preview/chunking now prefers full lines (more chunks rather than mid-line truncation) and status includes `date: ...` for summary jobs.
- AI summaries/epochs: harden dedupe and prevent duplicate record lines in base year epoch job inputs.
- Epochs view: exclude synthetic pinned-today clones from inherited mark child voting/visibility.
- AI contexts/settings: remove persisted reduce context template; reduce uses the Summary context template.
- Tests/docs: add regression coverage and update the context pack.

## [0.3.26] - 2026-02-25
- Epoch AI prompts: add bucket-aware guidance via `{{bucket}}` (day/2weeks: concrete topics; month/year: broader themes).
- AI bridge: status line no longer repeats job kind for `epoch://...` inputs.

## [0.3.25] - 2026-02-25
- Reset: cancel in-flight AI enqueue loops so Reset > All never announces queued AI summaries.
- AI summaries: suppress the “AI summaries: queued …” notice when a generate-all pass is canceled mid-run.
- AI bridge: reduce false disconnected flapping when idle to avoid duplicate Chrome tabs.

## [0.3.24] - 2026-02-25
- AI progress: status-bar summarization progress now uses live totals and resets per completed batch, so each new run starts from `0/N`.
- Similarity/topics progress: removed batch denominator snapshots and switched to live `processed + pending` totals.
- Maintenance: pruned additional unused exports/helpers and aligned context docs/tests with the updated progress behavior.

## [0.3.23] - 2026-02-23
- Timeline search: show up to 7 record suggestions, show most recent records for empty/invalid queries, always include a Filter action for invalid input, and improve suggestion formatting (title + smaller `folder ⸱ summary`).

## [0.3.22] - 2026-02-22
- AI summaries: fix `Summarize AI` from the pinned-today context menu so it reliably enqueues work for the original entry date.

## [0.3.21] - 2026-02-22
- AI bridge/epochs: improve cascaded epoch progress accuracy (initial totals reflect full bucket hierarchy; Remaining tok accounts for all buckets), remove redundant epoch queued notice after summaries, and ensure cancel clears planned cascade queue state.

## [0.3.20] - 2026-02-22
- Desktop status bar: refined progress UX (standardized task labels, AI bridge progress shows `Summaries… X/Y` or `Epochs… X/Y`, aria-label aggregates all active progress texts, and cancel clears AI bridge queue/planned work).

## [0.3.19] - 2026-02-21
- Timeline: opening a note now snaps to the newest visible record for that file (ignores cursor line), including on mobile when the cursor line is unavailable.

## [0.3.18] - 2026-02-21
- Indexing: frontmatter-derived parsed date records now anchor to stable non-blank lines, so frontmatter-only notes (including trailing blank lines) still appear reliably.
- AI bridge: disable language detection and always use English (`en`) for summarization.

## [0.3.17] - 2026-02-21
- Export Epochs (Pro): export respects the current view filters (search + toggles) and preserves epoch mark colors in standalone HTML.
- Export Epochs (Pro): capture theme mark palette CSS variables so exported colors match the active Obsidian theme.
- Export Epochs (Pro): exported timeline scroll uses default inertia (less stiff).
- Export: treat `epochgram-*.html` exports as non-text for AI/epoch generation (not summarized), while keeping them visible in the timeline without enabling Attachments.

## [0.3.16] - 2026-02-21
- Review state: preserve `hidden` on file edits (indexing no longer auto-demotes hidden files to Draft; Reset still forces Draft).

## [0.3.15] - 2026-02-20
- YAML frontmatter: parse date-like values from parsed frontmatter for timeline entries (excluding top-level `date`, which remains the anchor `dateprop`).
- Timeline search: index YAML frontmatter fields (all keys except top-level `date`) and avoid indexing raw YAML blocks as content.
- Timeline UI/search: hide frontmatter-derived date records by default; use `!prop` / `!p` to include them.

## [0.3.14] - 2026-02-20
- Timeline search: apply query now uses `Alt+Enter` (`Option+Enter` on macOS) instead of `Shift+Enter`.
- Docs/UI: updated modifier labels for macOS (`Cmd`, `Option`).
- Timeline: zoom now supports `Cmd` + wheel on macOS (in addition to `Ctrl`).

## [0.3.13] - 2026-02-20
- Indexing: avoid rewriting `epochgram-index.json` on startup when deferred resync finds no-op changes (desktop + mobile).

## [0.3.12] - 2026-02-20
- Timeline: add YAML frontmatter `date` as an anchor date source (`dateprop`) with priority `namedate > dateprop > cdate`.
- Content date extraction: skip YAML frontmatter so `date:` doesn’t get treated as a content date.

## [0.3.11] - 2026-02-20
- Timeline search: stabilize on-disk cache (`epochgram-search.json`) so it’s deterministic and only rewrites after real index mutations (prevents Obsidian Sync upload churn).

## [0.3.10] - 2026-02-20
- Timeline search: show `{query} - {n} results` / `{query} - no results` in the bottom search control, and keep the count synced as the timeline updates.
- Timeline search: always include a `(Clear)` suggestion and treat unknown `!directives` as invalid (no suggestions/results/records).
- Similarity: process newest-first more consistently and fix MiniSearch cache load behavior.

## [0.3.9] - 2026-02-20
- Timeline search: support additional one-token numeric date ranges (e.g. `2026.01.01-2026.01.30`, `22.02.2026-25.02.2026`, `01-02-2026-28-02-2026`) and render the final action as `(Filter <query>)`.

## [0.3.8] - 2026-02-20
- Timeline search: `Enter` now opens the selected match without applying the filter; added a `(Filter)` suggestion to apply the current query.
- Timeline search: removed the fallback “current input” suggestion when there are no matches.

## [0.3.7] - 2026-02-19
- Timeline search: expanded directives + full-text support, with improved exact-first matching and caching/settings updates.
- Search: additional behavior and UX refinements.
- Epochs/Pro: tightened epoch handling and enforced Pro-only gating for relevant tracked-changes/directive features.
- Similarity: default similarity threshold is now 0.79.
- UI: updated favicon.

## [0.3.6] - 2026-02-17
- Epochs view: fixed **Regenerate epoch** to enqueue regeneration for the selected epoch (not the next adjacent one).

## [0.3.5] - 2026-02-17
- Epochs view: preserve explicit line breaks in epoch summaries while keeping drop-caps enabled.
- Epochs view: inherited mark coloring now respects active filters (e.g. hidden tracked changes no longer mark epochs).
- Tests: added regression coverage for epoch newline rendering and filter-aware epoch coloring.

## [0.3.4] - 2026-02-17
- AI bridge: set the bridge page favicon to the Epochgram logo (mobile-style rounded background) and updated the page title.

## [0.3.3] - 2026-02-17
- AI bridge: updated bridge page copy to refer to the Epochgram plugin.

## [0.3.2] - 2026-02-17
- Timeline search: recent search history is now persisted (last 3 queries) with suggestions, including a `(Clear)` action.
- Timeline search: search only applies on Enter (typing no longer updates the timeline live).
- Mobile/touch: search control stays muted on long-press/focus (no stuck hover styling).
- Maintenance: Rebuild/Reset popups now use tighter setting-item spacing.
- Desktop mobile-emulation: avoid loading Node `http` in mobile mode, preventing the "Attempting to load NodeJS package: http" error.

## [0.3.0] - 2026-02-16
- Commands/settings/docs: renamed “Open view” to “Open timeline”.
- Epochs: standardized user-visible capitalization (“Generate Epochs”, “Export Epochs”, “missing Epochs”).
- Export (Pro): added **Export Epochs** single-file HTML export (epochs-only timeline snapshot).
- Indexing: exported `epochgram-*.html` / `epoch-*.html` / `epochs-*.html` files are ignored.
- Desktop status bar: added hover hints and accessibility labels for the progress indicator and AI bridge icon.
- AI bridge: retry transient summarize failures up to 3 times.
- Hover: suppress hover updates during mouse drag/pan to prevent flicker.
- Topics: “Topic” menu item now shows why it’s disabled (Pro / enable Topic threshold).

## [0.2.9] - 2026-02-15
- Timeline search: prefix with `!` for exact phrase matching.
- Timeline search: opening the search popup (including via command) no longer snaps/autoscrolls the timeline.
- Timeline search: improved digit-suffixed matching (e.g. `epochgram29` no longer matches `epochgram`).

## [0.2.8] - 2026-02-15
- Timeline: added a session-only fuzzy search filter with a bottom-center control and a standard Obsidian search dialog.
- Commands: added **Search timeline** (`search-timeline`) to open the timeline search popup.
- Epochs view: packed/stacked epoch summaries no longer jump when panning the timeline.
- Mobile/touch: timeline search control hover styling no longer applies on touch devices.

## [0.2.7] - 2026-02-14
- Reviews (mobile): fixed review-state context menu taps so they apply the new state and dismiss the menu.

## [0.2.6] - 2026-02-14
- AI bridge: if "Open Bridge AI on startup" is disabled, the bridge page stays open across Obsidian restarts and auto-recovers instead of requiring a manual reload.
- AI bridge: removed Start/Stop controls; the bridge page now runs continuously while open.
- AI bridge UI: clearer API/connection status, and error output is boxed + hidden when empty; transient network errors clear on reconnection.
- Related context: similar-note context is now sourced across all dates and rendered deterministically in a grouped nested list.
- Prompts: default summary/reduce templates now treat URLs as references only (do not infer dates/prices/IDs from URL query parameters).
- Epochs: epoch generation appends a non-text attachment path list, and the epochs view now respects active filters.

## [0.2.5] - 2026-02-13
- Tracked changes: manual **Summarize AI** results no longer revert back to the normal summary during follow-up processing (e.g., epoch regeneration / persistence refresh).
- Ordering: AI summary generation and epoch generation now process newest → oldest more consistently (including epoch job sorting and date-key enqueue normalization).
- Epochs: fixed deferred (after-AI) bucket cascade queue merging to prevent starvation of higher buckets (e.g. `3months`).
- Epochs: made overlap / truncation date math UTC-deterministic to avoid timezone-dependent drift.
- AI summaries: reject placeholder outputs like "No text provided" and fall back to the normal summarization behavior.
- AI summaries: trim a trailing single dot from AI output (while keeping ellipses).
- Prompts: updated default epoch context template.

## [0.2.4] - 2026-02-12
- Epochs: updated supported epoch buckets to `day`, `2days`, `4days`, `week`, `2weeks`, `month`, `3months`, `6months`, `year` (removes the legacy `3days` bucket).
- Epochs: fixed deferred (after-AI) cascade regeneration so it continues to higher buckets (e.g. `year`) even when an intermediate bucket has nothing to generate.
- Reviews: fixed reviewed items showing as Draft after disk reload by preserving `reviewState` and `contentHash` during index normalization.
- Tests: added regression coverage for epoch regeneration cascade and review-state preservation.

## [0.2.3] - 2026-02-11
- Epochs: maintenance rebuild now regenerates epochs as a day→…→year cascade, and sequences epoch generation after AI summaries when both are selected.
- Epochs: epoch inputs exclude attachments/non-text paths and include per-item file labels for better context.
- Epochs: parent buckets now include overlapping immediate-child epochs (fixes missing month/year epochs for sparse historical data near period boundaries).
- Internal: removed the legacy rank system and refreshed related tests/docs.

## [0.2.2] - 2026-02-11
- Reviews: fixed the summary context menu showing the wrong current review state (normalization + path mismatch edge cases).
- Rendering: tagged rows now use a subtle background tint (no tag icon), with hover/unhover animations sizing correctly.

## [0.2.1] - 2026-02-10
- Build/dev: moved plugin source under `src/` and updated build/test wiring accordingly.
- TypeScript: enabled `strict` type-checking (repo-internal; no user-facing change intended).
- AI bridge: Chrome opener is now lazy-loaded so Node-only modules are not loaded on mobile.

## [0.2.0] - 2026-02-10
- Indexing/summary: when summary flattening results in empty text, fall back to the original (pre-flatten) text (covers both note-start and AI summary inputs).

## [0.1.39] - 2026-02-10
- AI bridge: reduce duplicate Chrome tabs after inactivity by throttling silent/background re-opens (unless the bridge page explicitly closed).
- AI bridge (macOS): opening the bridge page no longer spawns extra Chrome instances/tabs.
- Sync/tracked changes: stronger snapshot normalization (BOM + newline variants + Unicode line separators) to avoid sync-only diffs.
- Sync/tracked changes: after long offline gaps, bucket tracked changes by file mtime day to avoid attributing them to “today”.
- Reviews: avoid resetting review state to Draft due to sync-only content normalization.
- Mobile/touch: improved touch pan inertia handling and fixed extra scroll.
- Marks: refresh mark color palette from Obsidian theme variables; sort mark color shades by luminance.
- Commands/docs/tests: renamed “Open timeline” command and refreshed related docs/tests.

## [0.1.37] - 2026-02-07
- Mobile/touch: fixed long-press context menu hover flicker (hover no longer visually drops while the menu opens).
- Mobile/touch: date label hover hit area now matches the padded long-press menu target.
- Rendering/perf: avoid suppressing hover work while a menu is open or touch-hover is pinned.
- UI: collapsing the panel clears persistent hover to avoid stale highlights.
- Scroll-nav/hover: additional stability fixes (hover tap behavior, scroll-nav hover interactions).
- Misc: fixed a context menu targeting edge case and tightened date range handling.

## [0.1.36] - 2026-01-25
- Epochs: allow **Edit epoch…** (manual override) on mobile.
- UI: enforce modal title ellipsis for long “Edit epoch …” titles.
- Fix: changing Summary length no longer clears manual summaries.
- Docs/tests: marked the manual test case checklist as complete.

## [0.1.35] - 2026-01-22
- Startup: timeline snaps (no animation) to the opened markdown file (or Today if none) using a consistent focus anchor.
- Follow-focus: opening files from the file explorer now triggers auto-scroll; timeline interactions suppress follow-focus briefly to avoid “fighting”.
- Scroll-nav: improved parity and anchoring (Alt+wheel, Alt+Up/Down), including startup anchoring and “last clicked” anchoring.
- Hover/rendering: fixed unhover/hover transitions when moving between items; improved row-local right-shift behavior.
- Mobile: improved long-press hit targets for date labels in the epochs view.
- Epochs: added **Edit epoch…** (manual override) and regeneration semantics (Regenerate clears manual text back to auto); parent epoch regeneration reflects manual child text.
- Epochs: generated epoch summaries trim decorative boundary punctuation (e.g. quotes/em-dashes) while keeping normal sentence punctuation.
- AI bridge: Clear queue reliability improved (clears current + planned work; planned regeneration does not resume).
- Indexing/maintenance: reduced index size (removed snapshots) and improved rebuild/reset behavior.
- Test suite/docs: expanded unit coverage and refreshed the manual test case suite; added a manual testing matrix.

## [0.1.34] - 2026-01-16
- Timeline: clicking records (including dense bars, placeholders, and `+n` compact rows) no longer auto-zooms.
- Dense bars: when a single bar represents multiple records, clicking prefers the earliest marked record (explicit or inherited).
- Build: `npm run build:production` no longer fails when the repo is the vault plugin directory (deploy overwrites packaged files in-place).

## [0.1.33] - 2026-01-16
- Date parsing: fixes false positives (including accidental “today” matches) and supports years 1001–2099.
- Pinning: pinning a record that’s already on Today pins immediately (no animation).
- Epochs: when Generate Epochs is off, the epochs filter/view toggle is disabled (even if epoch entries already exist).
- Build: `npm run build:production` preserves existing `data.json` on deploy and deploys to all detected vault plugin dirs.

## [0.1.32] - 2026-01-16
- Mobile: notes auto-created/opened on startup (Default file to open: New note/Daily note) now show up on the timeline without requiring reload.
- Startup: register file listeners earlier and opportunistically index newly opened files that are missing from the current index.

## [0.1.31] - 2026-01-16
- Maintenance: clarify why “Epochs” rebuild is disabled (requires Pro + Desktop + Generate Epochs).

## [0.1.30] - 2026-01-16
- AI bridge: fixed a typo in the Chrome fallback notice.

## [0.1.29] - 2026-01-16
- Build: `npm run build:production` no longer fails in CI when `AKUPARA/.obsidian/plugins` isn’t present; it still packages to `production/epochgram` and skips vault deploy.

## [0.1.28] - 2026-01-16
- Date parsing: require explicit year (avoid inferred-year dates and weekday/time false positives).
- Similarity: allow model downloads from Hugging Face hosts in the similarity worker.
- AI bridge: clearer error notice when Chrome can’t be opened automatically.
- Settings: Pro panel description rendered as a paragraph.
- Mobile: License key + Activate stack full-width.
- Build: `npm run build:production` now deploys artifacts into `AKUPARA/.obsidian/plugins/epochgram`.

## [0.1.27] - 2026-01-15
- UI: fixed mark menu color dot icons not rendering on macOS.

## [0.1.26] - 2026-01-15
- UI: increased the small font size to match the default UI font.

## [0.1.25] - 2026-01-15
- Settings: added “Enable animation” toggle (default on) to disable all timeline/canvas animations.
- Rendering: improved vertical centering so single-line records align with the day dot.
- Settings UI: Pro panel now wraps only the Pro features list.

## [0.1.24] - 2026-01-15
- Hover: fixed date circle/label hover animation + reliable unhover when zoomed out (below `SUMMARY_MIN_SCALE`).
- Metadata: updated minimum Obsidian version to 1.11.4.

## [0.1.23] - 2026-01-15
- Hover: right-column shifts are now row-local (only overlapping rows shift).
- Rendering: placeholder stroke width now matches dense-bar width (full available column width).

## [0.1.22] - 2026-01-15
- UI: adjusted the Pro panel styling to avoid forcing a background color.

## [0.1.21] - 2026-01-15
- Timeline: click-to-zoom parity improvements (dense bars / +n), plus consistent hover suppression while zooming.
- Reviews filter: tri-state Drafts+reviewed / Draft / All, persisted across restarts.
- Delete: respects Obsidian “Confirm file deletion” (promptDelete) via an in-plugin modal.
- Settings: regrouped layout into setting groups (Similarity, Generative AI), with License key + Track changes combined.
- Mobile: fixed filter button background getting stuck transparent after toggling.
- Rendering: tag icon no longer scales on hover; wrapping avoids rendering “⸱” at line start/end.

## [0.1.20] - 2026-01-12
- Mobile: removed the separator above “Topic” in the entry context menu.

## [0.1.19] - 2026-01-12
- Reviews: replaced per-entry hide/show with per-file Draft/Reviewed/Hidden model (Draft is default; edits reset to Draft; Hidden is filtered).
- UI: entry context menu reflects current review state with icons and a simplified Reviews submenu.
- Rendering: Draft rows render italic consistently (including pinned/active/bold cases).
- Commands: “Review all” promotes Draft → Reviewed for all indexed files.
- Reset: “Reviews” resets all files back to Draft.
- Fix: editing a pinned file now correctly resets it back to Draft.
- Fix: resetting to Draft now forces re-aggregation for older index data missing review state, preventing non-italic Draft rows.

## [0.1.18] - 2026-01-12
- AI: “Generate missing …” prompts are shown only when there is actually missing work, and include job counts.
- AI summaries: disabling Auto summarize no longer removes stored AI summaries.
- Epochs: improved missing detection so existing epoch summaries are not re-generated.
- Epochs: disabling Generate Epochs no longer deletes already-generated epoch summaries.
- Similarity: disabling semantic threshold no longer masks existing embeddings after re-enabling.

## [0.1.17] - 2026-01-09
- Marks: recompute inherited mark colors after reload.
- Timeline: improved active-file visibility detection and off-screen focus scroll anchoring.
- Hover (normal view): full-width hover summary (multiline wrap), epochs-like animation, minimized right-shifts, and fixed TDZ crash.
- Normal view: unhovered summaries clip/expand safely to available timeline width (no overflow/overlap).
- AI bridge: UX fixes (stable tok/s, reduced flicker, keep last result, clear input preview) and stuck-job resume after reload.
- AI queue/epochs: re-queue deduping, plus queued notices with job counts that can open the bridge page.
- Ranking: deterministic GLOBAL rank (0..1) per note, persisted; integrates into AI summaries and epoch selection.

## [0.1.16] - 2026-01-09
- Similar navigation: ALT+Up/Down, Alt+scroll, and two-finger tap now traverse all similar records without skipping.
- Similar navigation: focusing order matches the timeline order.

## [0.1.15] - 2026-01-07
- AI bridge: clarified the Summarizer API availability note in the local docs.

## [0.1.14] - 2026-01-07
- Topics: added topic-aware grouping and mark inheritance (`!topic`, `#topic`, computed topics), including stable “unique topic” behavior.
- Persistence: epochs and AI summaries are persisted in `epochgram-summaries.json` (`v: 2`) and applied at runtime.
- Persistence: `epochgram-index.json` is now disk-normalized (does not persist epoch entries or AI summary fields).
- Inherited marks: fixed cache update flicker by dropping only affected cache entries.
- Notices: reduced noisy desktop delayed-task notices.

## [0.1.13] - 2026-01-07
- Epochs view: improved drop-cap wrapping in narrow columns (prefix can wrap by words; rest wraps cleanly without breaking short words into letters).
- Hover overlay: matches the same drop-cap boundary and wrapping as non-hover.

## [0.1.12] - 2026-01-07
- Similarity backend: reverted to loading ONNX Runtime WASM from jsDelivr using `ONNX_RUNTIME_WEB_VERSION` (release artifacts ship only the 5 top-level files, so `vendor/` can’t be relied on).

## [0.1.11] - 2026-01-07
- Release artifacts: fixed GitHub Actions packaging to ship ONNX Runtime WASM files needed for the similarity backend (prevents “No available backend found”).

## [0.1.10] - 2026-01-07
- Mobile: fixed startup crash by drastically reducing bundle size (stop inlining ONNX Runtime WASM into `main.js`).
- Epochs view: fixed drop-cap gap behavior in narrow columns.

## [0.1.9] - 2026-01-07
- AI bridge: removed unload handler to avoid Chrome policy warnings/errors.

## [0.1.8] - 2026-01-07
- Startup: auto-opens Epochgram on desktop after activation.
- Mobile: avoids auto-opening Epochgram on startup to prevent crashes.

## [0.1.7] - 2026-01-07
- Metadata: updated plugin description.

## [0.1.6] - 2026-01-07
- Metadata: replaced remaining Ukrainian strings with English.

## [0.1.5] - 2026-01-07
- Fix AI summary timeline behavior: manual AI summaries now display regardless of Auto summarize, and clearing Edit summary also clears the stored AI summary.

## [0.1.4] - 2026-01-07
- Compliance: removed runtime download of ONNX Runtime WASM (bundled/embedded) and blocked network fetches in the similarity worker.
- Similarity/topics: improvements and fixes for semantic matching + topic classification.
- AI bridge: fixes and performance improvements.
- UI/UX: updates to mark commands and file context menu behavior.
- Build/CI: GitHub Actions now uses the production build pipeline and includes a guard against reintroducing the ONNX WASM CDN fallback.

## [0.1.3] - 2025-12-25
- Summarize AI.
- Generate Epochs.
- Similarities.
- Different mark colors.
- Edit summary.
- Edit topic.
- Moved features to free version.
- Updated context menus and ui.
- Filenames are added at render time (not stored in summaries).
- A lot of other changes and fixes.

## [0.1.2] - 2025-12-15
 - Epochs: added **Edit epoch…** (manual override) and regeneration semantics (Regenerate clears manual text back to auto); parent epoch regeneration reflects manual child text.
 - Epochs: generated epoch summaries trim decorative boundary punctuation (e.g. quotes/em-dashes) while keeping normal sentence punctuation.
 - Epochs: when Generate Epochs is off, the epochs filter/view toggle is disabled (even if epoch entries already exist).
- Fixed scrolling to previous/next record.
 - Maintenance: clarify why “Epochs” rebuild is disabled (requires Pro + Desktop + Generate Epochs).

 - Epochs: improved missing detection so existing epoch summaries are not re-generated.
 - Epochs: disabling Generate Epochs no longer deletes already-generated epoch summaries.
- Pins.
 - Epochs view: improved drop-cap wrapping in narrow columns (prefix can wrap by words; rest wraps cleanly without breaking short words into letters).
- Dense threshold.
 - Epochs view: fixed drop-cap gap behavior in narrow columns.
- Limit to one record per file per day (priority-based).
- Sorting order.
- Timeline icons and styles.
- Generated summary uses note name.
- Toggle hiding for all day entities.
- Daily context menu.
- Weekend styles.
- Prevented auto-scroll when the opened file already has a visible timeline entry.
- Removed unnecessary scrolling.
- Prevented accidental clicks during pinch zoom.
- Fixed opening multiple tabs for attachments.
- Adjusted the Move to... dialog.
- Improved track changes handling.
- Improved parsing of multiple dates in a line.
- Adjusted components available in the Pro version.
- Major refactor.

## [0.0.9] - 2025-11-30
- Updated symbols for tracked edits and parsed date summaries.
- Settings text updates.
- Fixed styles of the Rename... dialog on mobile.

## [0.0.8] - 2025-11-29
- Improved context menu for timeline records and dates.
- Fixed track changes behavior.
- Removed duplicate filenames in hover titles when the summary is empty.
- Disabled extra scrolling for opened visible items.

## [0.0.7] - 2025-11-28
- First stable beta version.
